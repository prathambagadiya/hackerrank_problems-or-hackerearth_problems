#include<iostream>
using namespace std;
int main()
{
int n,l;
cin>>n>>l;
int a[n];

   for (int i=0;i<n;i++)
    {
    cin>>a[i];
    if(a[i]>=l)
      {
      printf("Yes\n");
      }
    else
      {
      printf("No\n");
      }
    }
}
